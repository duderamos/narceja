%h2 Domains

.container
  - if @domains.count == 0
    %p.alert.alert-warning
      There's no domain registered yet.
  - else
    %table.table.table-striped.table-bordered.table-hover
      %tr
        %th Name
        %th Description
        %th
      - @domains.each do |domain|
        %tr{ class: cycle('odd', 'even') }
          %td
            = domain.name
          %td
            = domain.description
          %td
            = link_to 'Edit', '#'
            = link_to 'Delete', '#'
